{
  "name": "feliciano-sellable-template",
  "version": "1.0.0",
  "description": "Vercel-ready hotel and restaurant website template with admin dashboard, bookings, Cloudinary images, and editable settings.",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "check": "node --check api/index.js && node --check server.js && node --check js/admin.js && node --check js/bookings.js && node --check js/gallery.js && node --check js/settings.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "dependencies": {
    "cloudinary": "^2.5.1",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {}
}
